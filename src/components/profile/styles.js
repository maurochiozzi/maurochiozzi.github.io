import styled from "styled-components";

const PictureFrame = styled.img`
  width: 200px;

  border-radius: 50%;
  background: white;

  display: block;

  margin-top: 20px;
  margin-left: auto;
  margin-right: auto;
`;

export default PictureFrame;
