import styled from 'styled-components'

const PictureFrame = styled.img`
    width: 150px;
    
    border-radius: 50%;
    border-style: solid;
    border-color: grey;
    border-width: thin;

    background: white;
`

export default PictureFrame;